import { Box } from '@gluestack-ui/themed'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function AccountPage() {
  return <Box bgColor="white" flex={1} as={SafeAreaView}></Box>
}
